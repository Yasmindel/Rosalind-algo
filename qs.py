def main():
    # Hardcoded input dataset
    numbers = [5, -2, 4, 7, 8, -10, 11]
    # Sort the numbers in ascending order
    sorted_numbers = sorted(numbers)
    # Print the resulting sorted numbers
    print(' '.join(map(str, sorted_numbers)))

if __name__ == '__main__':
    main()
